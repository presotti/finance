spring:
  application:
    name: finance

  datasource:
    url: jdbc:postgresql://db.cmpnbfyxfafpbysphmqy.supabase.co:6543/postgres?sslmode=require
    username: postgres
    password: hotra0-vetFez-nuwqeh
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update # Opções: update, create, validate, none
    show-sql: true # Mostra as queries no console (bom para debug)
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  # O Railway injeta a variável PORT. Isso é CRUCIAL.
  port: ${PORT:8080}